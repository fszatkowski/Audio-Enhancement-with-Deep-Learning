MODELS:
    - try different activations for last layer (hard tanh or unit)
    - train without noise and check effects
DEMO:
    ? add more example audio files or models
    ? beautify interface
PROJECT:
    - check if python scripts work with updated requirements
    - try if newer versions of torch work