MODELS:
    - check if dataset sampling online works (eg. load only random files for each epoch)
    - learning rate decay ?
DEMO:
    ? add more example audio files or models
    ? make interface prettier
PROJECT:
    - add tests for whole training pipeline, maybe check if torch 1.7 works for everything